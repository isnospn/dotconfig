return {
    "lukas-reineke/indent-blankline.nvim",
    main = "ibl",
    opts = {
      indent = {
        char = "⋮", -- vertical triple dots (unicode U+22EE)
      },
      scope = { enabled = true },
      exclude = {
        buftypes = { "terminal", "nofile" },
        filetypes = { "help", "dashboard", "lazy", "mason" },
      }
    },

    config = function(_, opts)
      require("ibl").setup(opts){
      }
    end
}
